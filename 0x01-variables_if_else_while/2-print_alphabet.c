#include <stdio.h>

/**
 * main - Entry point of the program
 *
 * Return: 0
 *
 */
int main(void)
{
	int alpha = 'a';

	while (alpha <= 'z')
	{
		putchar(alpha);
		putchar('\n');
		alpha++;
	}
		return (0);
}
